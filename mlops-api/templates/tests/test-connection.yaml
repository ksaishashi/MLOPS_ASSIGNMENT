apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlops-api.fullname" . }}-test-connection"
  labels:
    {{- include "mlops-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlops-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
